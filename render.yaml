services:
  - type: web
    name: liveavatar-lite-agent
    runtime: node
    plan: free
    rootDir: agent-server
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
